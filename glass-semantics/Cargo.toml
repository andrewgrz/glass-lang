[package]
name = "glass-semantics"
version = "0.1.0"
edition = "2024"

[dependencies]
glass-syntax = { path = "../glass-syntax" }

[dev-dependencies]
chumsky = { version = "0.13.0", features = ["default", "lexical-numbers"] }
